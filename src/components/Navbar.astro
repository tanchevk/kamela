---
---

<div class="navbar">
	<a href="/">
		<img src="/favicon.svg" alt="Kamela Logo"/>
	</a>
	<h2><a href="https://github.com/Theboiboi8" target="_blank">GitHub</a></h2>
</div>
<style is:global>
	.titlebar {
		box-sizing: border-box;
		position: fixed;
		will-change: scroll-position;
		display: flex;
		flex-direction: row;
		//flex-wrap: nowrap;
		justify-content: space-between;
		align-items: center;
		max-height: 4rem;
		padding-left: 15dvw;
		padding-right: 15dvw;
		width: 100dvw;
		height: 4rem;
		/*background: linear-gradient(
				rgba(var(--accent-dark), 11%),
				rgba(var(--accent-dark), 22%)
		);*/
		backdrop-filter: blur(.5rem);
		//border-bottom: .1rem solid rgba(var(--accent), 66%);
		z-index: 100;

		@media (max-device-width: 650px) {
			//flex-wrap: wrap;
		}
	}
	.titlebar * {
		max-height: 4rem;
		max-width: 50dvw;
		@media (max-device-width: 650px) {
			//flex-basis: 100%;
		}
		text-decoration: none;
		color: white;
	}
	.titlebar a:has(img) {
		display: contents;
	}
	.titlebar h2 {
		padding-right: 1rem;
	}
</style>
